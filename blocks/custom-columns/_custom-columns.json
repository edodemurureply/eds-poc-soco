{
  "definitions": [
    {
      "title": "Custom columns",
      "id": "custom-columns",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Custom columns",
              "key-value": true,
              "filter": "custom-columns",
              "model": "custom-columns"
            }
          }
        }
      }
    },
    {
      "title": "Single column",
      "id": "single-column",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Single column",
              "model": "single-column"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "custom-columns",
      "fields": [
        {
          "component": "text",
          "name": "custom-columns-size",
          "label": "Add column size class (S, M, L, XL)",
          "required": true
        }
      ]
    },
    {
      "id": "single-column",
      "fields": [
        {
          "component": "text",
          "name": "single-column-size",
          "label": "Add column size class (S, M, L, XL)",
          "required": true
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "custom-columns",
      "components": ["single-column"]
    }
  ]
}
